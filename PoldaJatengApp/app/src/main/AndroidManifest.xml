<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.poldajatengapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SkckActivity" android:windowSoftInputMode="adjustPan" />
        <activity android:name=".LapkerActivity2" android:windowSoftInputMode="adjustPan" />
        <activity android:name=".esimActivity" android:windowSoftInputMode="adjustPan" />
        <activity android:name=".Lapor" android:windowSoftInputMode="adjustPan" />
        <activity android:name=".Register"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login"
            android:theme="@style/Theme.AppCompat.NoActionBar"/>
        <activity android:name=".EditProfile" />
        <activity android:name=".Profile" />
        <activity android:name=".MainActivity"
            android:screenOrientation="portrait"/>
    </application>
</manifest>